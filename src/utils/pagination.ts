export const previous = (num: number) => {
    console.log('previous')
}

export const next = (um: number) => {
    console.log('next')
}

export const move = (num: number) => {
    console.log(num);
}